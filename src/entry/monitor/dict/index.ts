/**
 * 监控事件类型
 */
export enum EventType {
    JS_ERROR = 'js-error',
}

/**
 * 时间范围
 */
export enum DurationEnum {
    hour = 'hour',
    day = 'day',
    week = 'week',
    month = 'month',
    other = 'other',
}
