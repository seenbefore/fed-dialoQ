<template>    <div class="app-base-form__container <$ appConfig.cacheConfig.appBaseFormClassName $>">        <AppForm ref="appFormRef" :fields="fields" :show-foot-btns="<$ config.showFootBtns $>" input-align="<$ config.inputAlign $>" v-model="formModel" @cancel="cancel" @submit="submit"></AppForm>    </div></template><script lang="ts">import AppForm from '@/entry/<$ appConfig.cacheConfig.applicationName $>/components/global/app-form/index.vue'import { Component, Ref, Vue } from 'vue-property-decorator'@Component({    name: '<$ appConfig.name $>',    components: {        AppForm,    },})export default class <$ appConfig.name $> extends Vue {    @Ref('appFormRef')    public appFormRef!: AppForm    public get fields() {        return <$ data $>    }    public formModel: Record<string, any> = <$ formData $>    public cancel(): void {        console.log('取消提交')    }    public submit(): void {        console.log(this.formModel)    }}</script><style lang="less" scoped><% if appConfig.cacheConfig.appBaseFormClassName == 'app-base-form-reset' %>@import '~@/entry/<$ appConfig.cacheConfig.applicationName $>/styles/reset/app-base-form-reset.less';<% endif %>.app-base-form {    &__container {        height: 100vh;        width: 100vw;        padding: 16px;        box-sizing: border-box;        background-color: rgb(243, 245, 247);    }}</style>